# Common rules for every discovery lane

- Repo: /Users/cheycoulbourn/Documents/Agent.cy App. Read `docs/refinement/00-contract.md` first; it is binding. Then read `docs/refinement/01-page-inventory.md` for the surface list and slugs. Use those slugs when you name a page.
- You are a discovery lane. You do not modify any file outside `docs/refinement/`. You do not spawn subagents. You do not commit.
- `docs/archive/audit-2026-08/` is untrusted. You may open it for tips about where to look, but nothing from it counts as evidence. Every finding needs evidence produced by you in this pass: a file:line excerpt, a measurement you took, or a screenshot you captured.
- Findings use the format in the contract, numbered with your lane prefix. Severity: blocker (a beta tester would notice and be annoyed, or data is at risk), major (visible inconsistency or real cost), minor (cosmetic or internal).
- Every finding proposes a concrete fix. If a fix is a shared change (a new shared component, a token, a design.md rule), say so and name every site it would touch.
- Evidence files go under `docs/refinement/evidence/<lane>/`. Keep screenshots small (PNG from the simulator is fine).
- Both form factors matter. The phone shell is `ios/AgentCy/Views/Shell/AppShellView.swift`; the desktop shell is `DesktopAppShellView.swift` (Mac Catalyst, scheme "AgentCy Desktop"). Both appearances matter: light and dark.
- Tooling on this Mac: Xcode 26, xcodegen at /opt/homebrew/bin, node at /opt/homebrew/bin, no pnpm installed (use `npx -y pnpm@11.7.0 ...`), no ripgrep (use grep). Put /opt/homebrew/bin on PATH.
- A prebuilt Debug simulator app may exist at `/private/tmp/claude-501/-Users-cheycoulbourn-Documents-Agent-cy-App/f4f952ec-713d-4fbe-a094-33299db34639/scratchpad/DerivedData/Build/Products/Debug-iphonesimulator/AgentCy.app` after the baseline test run. Simulator: "iPhone 17 Pro". Boot with `xcrun simctl boot "iPhone 17 Pro"`, install with `xcrun simctl install booted <app>`, launch with `xcrun simctl launch booted com.agentcy.app -agentCyPreviewData -agentCyPreviewTab <tab>`, screenshot with `xcrun simctl io booted screenshot <path>`. Dark mode: `xcrun simctl ui booted appearance dark`. Other fixture flags exist in `ios/AgentCy/Preview/` (grep for `agentCyPreview`). A plain launch without fixture flags stops at the account gate. If you need your own build, use `-derivedDataPath` under `/private/tmp/claude-501/-Users-cheycoulbourn-Documents-Agent-cy-App/f4f952ec-713d-4fbe-a094-33299db34639/scratchpad` (never inside the repo; it is iCloud-synced and breaks signing).
- When done, reply with only: finding counts by severity, the paths you wrote, and blockers. Do not paste findings into the reply.
